# CSV 자동 진단 기능 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `@철수` 멘션 + CSV 첨부 시 봇이 KB 정책 글을 기준으로 파일의 형식·패턴 위반을 진단해 스레드에 답글을 준다.

**Architecture:** `handleEvent`의 `app_mention` 분기에 "CSV 첨부 감지 → `handleCsvDiagnosis` 우선 라우팅"을 추가한다. `handleCsvDiagnosis`는 다운로드→디코딩→정책fetch→LLM진단→답글 순으로 동작한다. 모든 변경은 `api/slack/events.ts` 한 파일에 집중된다.

**Tech Stack:** TypeScript, Node.js TextDecoder (인코딩), Anthropic SDK (Haiku), @slack/web-api, postgres

---

> **노트:** 이 프로젝트에는 테스트 러너가 없으므로 TDD 대신 구현 후 Slack 실 테스트로 검증한다.

---

### Task 1: SlackFile 타입 추가 + SlackEvent에 files 필드 추가

**Files:**
- Modify: `api/slack/events.ts:82-92` (SlackEvent 인터페이스)

- [ ] **Step 1: SlackFile 인터페이스 추가, SlackEvent에 files 추가**

`SlackEvent` 인터페이스 바로 위에 `SlackFile` 타입을 추가하고, `SlackEvent`에 `files` 필드를 추가한다.

```typescript
interface SlackFile {
  id: string
  name: string
  filetype: string
  url_private_download: string
  size: number
}

interface SlackEvent {
  type: string
  subtype?: string
  user?: string
  text?: string
  ts?: string
  channel?: string
  thread_ts?: string
  channel_type?: string
  bot_id?: string
  files?: SlackFile[]   // ← 추가
}
```

- [ ] **Step 2: 타입 체크 (빌드 에러 없는지)**

```bash
cd /Users/hwanghyebin/conductor/workspaces/testing-slack-bot/irvine
npx tsc --noEmit 2>&1 | head -30
```

Expected: 에러 없음 (또는 기존 에러와 동일)

- [ ] **Step 3: 커밋**

```bash
git add api/slack/events.ts
git commit -m "feat: SlackFile 타입 + SlackEvent.files 필드 추가"
```

---

### Task 2: handleEvent 라우팅 — CSV 첨부 감지 후 진단 분기 우선

**Files:**
- Modify: `api/slack/events.ts:121-129` (app_mention 분기)

- [ ] **Step 1: app_mention 분기에 CSV 우선 라우팅 추가**

기존 코드:
```typescript
  if (event.type === 'app_mention') {
    // 기존 멘션 기반 플로우 (스레드=저장 / 메인=질문)
    if (event.thread_ts) {
      await handleSave(event, slack)
    } else {
      await handleQuestion(event, slack)
    }
    return
  }
```

변경 후:
```typescript
  if (event.type === 'app_mention') {
    // CSV 첨부 → 진단 최우선 (저장/질문보다 앞)
    const csvFile = event.files?.find(
      (f) => f.filetype === 'csv' || f.name.toLowerCase().endsWith('.csv'),
    )
    if (csvFile) {
      await handleCsvDiagnosis(event, csvFile, slack)
      return
    }
    // 기존 멘션 기반 플로우 (스레드=저장 / 메인=질문)
    if (event.thread_ts) {
      await handleSave(event, slack)
    } else {
      await handleQuestion(event, slack)
    }
    return
  }
```

- [ ] **Step 2: 타입 체크**

```bash
npx tsc --noEmit 2>&1 | head -30
```

Expected: `handleCsvDiagnosis`가 없어서 에러 뜸 (정상 — Task 7에서 구현)

- [ ] **Step 3: 커밋 (함수는 stub으로 일단 붙이지 않음 — Task 7에서 한꺼번에 커밋)**

아직 커밋하지 않는다. Task 7 완료 후 함께 커밋한다.

---

### Task 3: downloadAndDecodeCsv 구현

**Files:**
- Modify: `api/slack/events.ts` (CSV 진단 섹션 신규 추가)

- [ ] **Step 1: downloadAndDecodeCsv 함수 작성**

`events.ts` 하단에 CSV 진단 섹션 주석과 함께 추가한다.

```typescript
// ──────────────────────────────────────────────────────────────
// CSV 자동 진단
// ──────────────────────────────────────────────────────────────

// 인코딩 시도 순서 (배송정보 CSV 정책 문서와 동일)
const DECODE_ENCODINGS = ['shift_jis', 'utf-8-sig', 'utf-8', 'euc-jp'] as const

// CSV 파일을 다운로드하고 디코딩한다.
// 반환: { ok: true, text } | { ok: false, reason: string }
async function downloadAndDecodeCsv(
  file: SlackFile,
  token: string,
): Promise<{ ok: true; text: string } | { ok: false; reason: string }> {
  let arrayBuffer: ArrayBuffer
  try {
    const res = await fetch(file.url_private_download, {
      headers: { Authorization: `Bearer ${token}` },
    })
    if (!res.ok) {
      return { ok: false, reason: `파일 다운로드 실패 (HTTP ${res.status})` }
    }
    arrayBuffer = await res.arrayBuffer()
  } catch (err) {
    return { ok: false, reason: `파일 다운로드 중 오류: ${(err as Error).message}` }
  }

  const bytes = new Uint8Array(arrayBuffer)
  for (const encoding of DECODE_ENCODINGS) {
    try {
      const decoder = new TextDecoder(encoding, { fatal: true })
      const text = decoder.decode(bytes)
      // replacement character(U+FFFD) 없으면 성공
      if (!text.includes('�')) {
        return { ok: true, text }
      }
    } catch {
      // 이 인코딩으로 디코딩 불가 → 다음 시도
    }
  }
  return {
    ok: false,
    reason:
      '파일 인코딩을 인식하지 못했어요. shift_jis / UTF-8 / EUC-JP 중 하나로 저장 후 다시 업로드해 주세요.',
  }
}
```

- [ ] **Step 2: 타입 체크**

```bash
npx tsc --noEmit 2>&1 | head -30
```

Expected: `handleCsvDiagnosis` 미정의 에러만 남아있음

---

### Task 4: fetchDeliveryPolicy 구현

KB `배송` 섹션의 정책 글을 조회해 프롬프트에 주입할 텍스트로 반환한다.

- [ ] **Step 1: fetchDeliveryPolicy 함수 작성**

`downloadAndDecodeCsv` 바로 아래에 추가:

```typescript
// KB `배송` 섹션의 정책 글을 가져온다. 없으면 빈 문자열.
async function fetchDeliveryPolicy(): Promise<string> {
  try {
    const rows = await sql<{ question: string; answer: string }[]>`
      SELECT q.question, q.answer
      FROM qa_items q
      JOIN sections s ON s.id = q.section_id
      WHERE s.name = '배송' AND q.is_deleted = FALSE
      ORDER BY q.created_at DESC
    `
    if (rows.length === 0) return ''
    return rows.map((r) => `Q: ${r.question}\nA: ${r.answer}`).join('\n\n')
  } catch (err) {
    console.error('fetchDeliveryPolicy error:', err)
    return ''
  }
}
```

---

### Task 5: diagnoseCsvWithLlm 구현

정책 글 + CSV 텍스트를 Claude(Haiku)에 넣어 형식·패턴 위반 JSON을 반환한다.

- [ ] **Step 1: Violation 타입 + diagnoseCsvWithLlm 함수 작성**

```typescript
interface CsvViolation {
  where: string   // 예: "헤더", "행 3", "파일"
  issue: string   // 짧은 위반 이름
  detail: string  // 상세 설명
}

interface DiagnosisResult {
  is_delivery_csv: boolean
  violations: CsvViolation[]
}

const CSV_ROW_LIMIT = 200

// 정책 글 + CSV 텍스트로 형식·패턴 위반을 LLM으로 진단한다.
async function diagnoseCsvWithLlm(
  policy: string,
  csvText: string,
  fileName: string,
): Promise<DiagnosisResult> {
  const lines = csvText.split('\n')
  const truncated = lines.length > CSV_ROW_LIMIT
  const csvSample = lines.slice(0, CSV_ROW_LIMIT).join('\n') + (truncated ? '\n... (이하 생략)' : '')
  const totalRows = lines.filter((l) => l.trim()).length - 1 // 헤더 제외

  const anthropic = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY, maxRetries: 5 })
  const completion = await anthropic.messages.create({
    model: CLAUDE_MODEL,
    max_tokens: 1024,
    system:
      '당신은 CSV 파일 형식 검증 도우미입니다. ' +
      '아래 정책 문서에 비춰 CSV의 형식·패턴 위반만 찾으세요. ' +
      '주문 상태(입금전취소·배송완료·결제전 등), 실제 주문번호 존재 여부, 배송사 DB 등록 여부는 CSV만으로 알 수 없으니 절대 판단하지 마세요. ' +
      '반드시 JSON 객체 하나만 출력하세요 (다른 텍스트 없이): ' +
      '{"is_delivery_csv": bool, "violations": [{"where": "헤더|행 N|파일", "issue": "짧은 이름", "detail": "상세"}]}',
    messages: [
      {
        role: 'user',
        content:
          `[정책 문서]\n${policy || '(정책 없음)'}\n\n` +
          `[CSV 파일: ${fileName}${truncated ? ` — 앞 ${CSV_ROW_LIMIT}행만 표시` : ''}]\n${csvSample}`,
      },
    ],
  })

  const rawText = completion.content[0]?.type === 'text' ? completion.content[0].text : ''
  const jsonMatch = rawText.match(/\{[\s\S]*\}/)
  if (!jsonMatch) {
    console.error('diagnoseCsvWithLlm: JSON parse fail', rawText)
    return { is_delivery_csv: true, violations: [] }
  }
  try {
    return JSON.parse(jsonMatch[0]) as DiagnosisResult
  } catch {
    return { is_delivery_csv: true, violations: [] }
  }
}
```

---

### Task 6: formatDiagnosisReply 구현

진단 결과 객체를 슬랙 답글 텍스트로 변환한다.

- [ ] **Step 1: formatDiagnosisReply 함수 작성**

```typescript
const STATE_CAVEAT =
  'ℹ️ 입금전취소·배송완료 등 주문 *상태* 기반 실패는 이 진단으로 확인할 수 없어요.\n실제 상태는 담당자 확인이 필요합니다.'

function formatDiagnosisReply(
  result: DiagnosisResult,
  fileName: string,
  rowCount: number,
  truncated: boolean,
): string {
  const header = `📋 *배송정보 CSV 진단 — ${fileName}* (${rowCount}행${truncated ? `, 앞 ${CSV_ROW_LIMIT}행만 검사` : ''})`

  if (!result.is_delivery_csv) {
    return (
      `${header}\n\n` +
      `⚠️ 첨부 파일이 배송정보 CSV 형식이 아닌 것 같아요.\n` +
      `기대 헤더: \`ご注文商品番号,配送会社名,請求書番号\`\n\n` +
      STATE_CAVEAT
    )
  }

  if (result.violations.length === 0) {
    return `${header}\n\n✅ 형식상 문제는 발견되지 않았어요.\n\n${STATE_CAVEAT}`
  }

  const list = result.violations
    .map((v) => `• [${v.where}] *${v.issue}* — ${v.detail}`)
    .join('\n')
  return `${header}\n\n⚠️ *형식·패턴 점검 결과*\n${list}\n\n${STATE_CAVEAT}`
}
```

---

### Task 7: handleCsvDiagnosis 구현 + 전체 커밋

모든 헬퍼를 조합해 오케스트레이션 함수를 완성하고 커밋한다.

- [ ] **Step 1: handleCsvDiagnosis 함수 작성**

```typescript
async function handleCsvDiagnosis(event: SlackEvent, csvFile: SlackFile, slack: WebClient) {
  const channel = event.channel!
  const ts = event.ts!
  const token = process.env.SLACK_BOT_TOKEN!

  // 1) 다운로드 + 디코딩
  const decoded = await downloadAndDecodeCsv(csvFile, token)
  if (!decoded.ok) {
    await slack.chat.postMessage({
      channel,
      thread_ts: ts,
      text: `📋 *배송정보 CSV 진단 — ${csvFile.name}*\n\n⚠️ ${decoded.reason}`,
    })
    return
  }

  const lines = decoded.text.split('\n')
  const rowCount = lines.filter((l) => l.trim()).length - 1
  const truncated = lines.length > CSV_ROW_LIMIT

  // 2) 정책 글 fetch
  const policy = await fetchDeliveryPolicy()

  // 3) LLM 진단
  let result: DiagnosisResult
  try {
    result = await diagnoseCsvWithLlm(policy, decoded.text, csvFile.name)
  } catch (err) {
    const status = (err as { status?: number })?.status
    const msg =
      status === 529 || status === 503
        ? '⏳ AI 서버가 잠시 혼잡합니다. 잠시 후 다시 시도해 주세요.'
        : status === 429
          ? '⏳ 잠시 후 다시 시도해주세요 (rate limit).'
          : '⚠️ 진단 중 오류가 발생했어요.'
    console.error('diagnoseCsvWithLlm error:', err)
    await slack.chat.postMessage({ channel, thread_ts: ts, text: msg })
    return
  }

  // 4) 답글
  const text = formatDiagnosisReply(result, csvFile.name, rowCount, truncated)
  await slack.chat.postMessage({ channel, thread_ts: ts, text })
}
```

- [ ] **Step 2: 타입 체크 — 에러 없어야 함**

```bash
npx tsc --noEmit 2>&1 | head -30
```

Expected: 에러 없음

- [ ] **Step 3: 전체 커밋**

```bash
git add api/slack/events.ts
git commit -m "feat: CSV 자동 진단 기능 (handleCsvDiagnosis)"
```

---

### Task 8: 슬랙 실 테스트

- [ ] **Step 1: dev:api 재시작 (코드 변경 반영)**

houston 디렉터리에서:
```bash
# 기존 프로세스 종료 후 재시작
pnpm dev:api
```

- [ ] **Step 2: ngrok URL이 Slack Event Subscriptions에 등록돼 있는지 확인**

`https://cb74-59-10-82-65.ngrok-free.app/api/slack/events` (또는 현재 ngrok URL)

- [ ] **Step 3: Slack에서 정상 CSV 테스트**

`upload_ship_sample (3).csv` 파일을 첨부하고 `@철수` 멘션:

```
@철수 배송정보 CSV 업로드 실패해요. 확인해줘.
[upload_ship_sample (3).csv 첨부]
```

기대 응답:
```
📋 배송정보 CSV 진단 — upload_ship_sample (3).csv (18행)

✅ 형식상 문제는 발견되지 않았어요.

ℹ️ 입금전취소·배송완료 등 주문 상태 기반 실패는 이 진단으로 확인할 수 없어요.
```
(3행이 입금전취소지만 봇은 상태를 모르므로 형식 문제 없음으로 답해야 정상)

- [ ] **Step 4: /tmp/vercel-dev.log로 분기 확인**

```bash
tail -20 /tmp/vercel-dev.log
```

`[event] type=app_mention`과 다운로드/LLM 로그가 찍혀야 함.

- [ ] **Step 5: 비-배송 CSV 테스트**

임의 CSV를 첨부해 `is_delivery_csv: false` 경로 확인:

```
@철수 이거 확인해줘.
[아무 CSV 첨부]
```

기대: "배송정보 CSV 형식이 아닌 것 같아요" 메시지

---

## 파일 변경 요약

| 파일 | 변경 내용 |
|---|---|
| `api/slack/events.ts` | SlackFile 타입, SlackEvent.files, handleEvent 라우팅, handleCsvDiagnosis + 4개 헬퍼 추가 |

## 전제 조건 (이미 완료)

- [x] Slack 앱에 `files:read` 스코프 추가 + 재설치
- [x] KB `배송` 섹션 + CSV 정책 글 저장 완료
- [x] 설계 문서 커밋 (`db9b0b1`)
